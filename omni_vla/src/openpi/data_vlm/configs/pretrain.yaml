recon:
  dataset_names:
  - recon_mix
  dino_image_transform_args:
    # image_stride: 14
    # max_image_size: 518
    # min_image_size: 518
    # max_pixels: 2_007_040
    # mode='crop',
    target_size: 224
  frame_sampler_args:
    max_num_frames: 12
    min_num_frames: 8
  is_mandatory: true
  shuffle_lines: True
  shuffle_seed: 0
  num_used_data:
  - 8000000
  weight: 1
